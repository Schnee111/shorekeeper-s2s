{
  "name": "shorekeeper",
  "private": true,
  "version": "0.3.0",
  "description": "Shorekeeper — voice-first multi-agent AI assistant (monorepo). TS workspace: apps/client + packages/*; Python apps via uv (standalone).",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "test:e2e": "node scripts/e2e/comprehensive-test.mjs",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "eslint": "^9.17.0",
    "prettier": "^3.4.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0",
    "vitest": "^2.1.8",
    "@eslint/js": "^9.17.0",
    "js-yaml": "^4.1.0"
  },
  "engines": {
    "node": ">=22.18"
  },
  "type": "module"
}
